@{
    //Layout = "_Layout";
    /*   Layout = null*/;
    if (User.IsInRole("Admin"))
    {
        Layout = "_AdminLayout";
        System.Diagnostics.Debug.WriteLine("Using Admin layout!!");
    }
    else if (User.IsInRole("Customer"))
    {
        Layout = "_AfterCustomerLoginPage";
        System.Diagnostics.Debug.WriteLine("Using Customer Layout!");
    }
    else
    {
        //Layout = "_Layout";
        Layout = "_CustomerLayout";
        System.Diagnostics.Debug.WriteLine("Using before login layout!!");
    }
}
